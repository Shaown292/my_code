import 'package:flutter/material.dart';

class RouteTile extends StatelessWidget {
  const RouteTile({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [

      ],
    );
  }
}
