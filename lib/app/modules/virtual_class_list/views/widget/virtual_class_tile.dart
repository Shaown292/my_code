import 'package:flutter/material.dart';

class VirtualClassTile extends StatelessWidget {
  const VirtualClassTile({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
