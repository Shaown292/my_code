import 'package:flutter/material.dart';

class AdminFeesInvoiceTile extends StatelessWidget {
  const AdminFeesInvoiceTile({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [

      ],
    );
  }
}
